import { Grid } from '@mui/material'

const Footer = () => {
  return (
    <Grid container>
        <Grid size={12} justifyContent={'center'}>
            
        </Grid>
    </Grid>
  )
}

export default Footer